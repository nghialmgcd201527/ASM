{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    <div class="form-group">
        <label for="author_id">Author ID:</label>
        {{ form_widget(form.author_id,{
            'attr':{
                'class':'form-control',
                'placeholder': 'Author ID'
            }
        }) }}
    </div>

    <input type="submit" name="edit_author" value="Edit" class="btn btn-primary"/>
    {{ form_end(form) }}
{% endblock %}
